# 8.16 组会汇报照念稿

> 对应文件：`8.16组会_项目阶段进展汇报.pptx`
>
> 建议汇报时长：约 4–6 分钟。下面基本可以直接照着念，遇到老师打断时按页继续即可。

---

## 第 1 页｜封面（约 30–40 秒）

各位老师、同学好，我汇报一下我们这个国创项目从上次组会到现在的阶段进展。

我们项目的题目是“基于 SegFormer 的骨科 CT 影像智能分割与三维重建研究”。

和上次相比，这一阶段最大的变化是：之前主要还在做方案设计、环境搭建和代码框架，现在已经把真实 CT 数据真正接进来了，并且把数据预处理、模型训练链、评价、Web 展示和三维重建这些关键环节基本串了起来。

目前已经有 10 例真实 CT 和对应标签落盘，10 例自动审计全部通过，项目自动化测试目前是 38 个全部通过。

所以现在项目的主要瓶颈，已经不再是“代码能不能跑、有没有真实数据”，而是下一步怎么尽快完成正式 GPU baseline，拿到第一版可以写进论文的真实实验指标。

---

## 第 2 页｜当前总体进度（约 1 分 20 秒）

这一页主要汇报一下目前各个模块的整体状态。

首先是数据预处理部分，目前完成度大约 90%。现在已经实际取得了 10 例 CTSpine1K 的真实 CT 和标签，并且完成了统一的标准化处理，包括 1 毫米重采样、HU 裁剪、标准化、骨窗增强以及标签的最近邻重采样，同时生成了三视图质量控制图。自动的几何、标签和标准化审计结果是 10 比 10 全部通过。

第二是模型适配，目前大约 55%。SegFormer3D 已经接入项目，我们也把骨科 CT 需要的 Region、Boundary 和 Topology 联合损失，以及困难样本增强、不确定性和局部精修这些模块做出了工程版本。但是这里要强调，目前这些模块只是“已经实现”，还没有正式训练后的消融结果，所以暂时不能说它们一定带来了精度提升。

第三是训练和评估框架，大约 85%。现在训练的数据加载、AdamW、学习率调度、checkpoint 保存、sliding-window 推理，以及 Dice、HD95、ASSD 等评价指标都已经接好了。并且我们已经在真实病例上做过一次单 patch 的 forward、backward 和参数更新，说明真实数据到模型训练这一条链路是通的。

Web 原型目前大约 55%，已经可以做上传、质量检查和 axial、coronal、sagittal 三视图 MPR。三维重建大约 48%，现在已经可以把真实标签通过 Marching Cubes 转成物理空间的 PLY 网格。

所以这一阶段可以概括成一句话：工程链路已经基本跑通，下一阶段要把这些工程成果转换成正式、可复现的实验结果。

---

## 第 3 页｜已完成的关键工作与工程证据（约 1 分 40 秒）

这一页主要展示几个现在已经能够实际复核的成果。

第一块是真实数据链路。我们现在已经有 10 例真实 CT 和标签，不再只使用合成数据做测试。每个病例都走了统一的 pipeline，包括 1 毫米重采样、HU 处理、case-wise z-score、骨窗以及标签同步处理。左边这张图就是其中一个真实病例生成的 QC 图，可以同时看三个方向的切片、骨窗以及标签叠加，用来检查图像和标签有没有错位。

第二块是模型和训练框架。SegFormer3D 的 adapter 已经做好，联合损失、困难增强、预测熵和 uncertainty ROI refinement 也都已经实现。另外，我们已经在真实标准化病例上跑通过 CT 加骨窗双通道输入，完成 joint loss、反向传播和 AdamW 参数更新。这一步的意义不是证明模型精度高，而是证明“真实数据进入模型并完成一次完整训练更新”这一条链已经打通。

第三块是 Web 和三维重建。Web 现在已经可以显示三视图 MPR，也支持窗宽窗位调节。三维部分已经能够把真实标签转换成物理空间网格，其中一个病例导出的网格大约有 13 万个顶点、26 万个三角面。

这里我也特意把边界说清楚：当前展示的三维网格是公开数据的真实标签生成的工程结果，不是模型预测，也不是临床诊断结果。这样可以保证现在展示的每一个结果都是真实可追溯的，不会提前把随机权重或者真值结果包装成模型性能。

目前代码回归测试是 38 个 pytest 全部通过，Ruff 检查也全部通过，所以当前工程状态相对稳定，可以进入正式实验阶段。

---

## 第 4 页｜当前不足与下一步（约 1 分 20 秒）

最后这一页主要说目前还缺什么，以及接下来准备怎么推进。

现在第一个还没完成的是 10 例真实病例虽然已经自动 QC 通过，但是人工审核表还需要逐例确认和签字。

第二个问题是首个正式研究任务和标签定义还需要最终固定，比如是整体脊柱二分类、多椎体语义分割，还是更细的实例任务。这个确定以后，正式的 patient-level train、validation、test split 才能锁定。

第三个也是目前最关键的外部条件，就是 GPU。现在本机环境是 CPU 版 PyTorch，可以做开发和 smoke test，但是不适合正式的三维 CT 训练，所以接下来需要确认 NVIDIA GPU 或服务器环境。

等这三步完成以后，我们会先跑一个最干净的 CT-only SegFormer3D baseline，固定配置、split、checkpoint 和训练日志，然后用独立 evaluation 生成逐病例指标，正式报告 DSC、HD95、ASSD 和推理时间。

有了 baseline 以后，再按照顺序做消融，不一次把所有模块堆上去。具体顺序是：先加骨窗双通道，然后 Boundary Loss、Topology Loss、困难样本增强，最后再做 uncertainty refinement。这样每一步带来的效果都能够单独解释。

最后总结一下：目前系统、数据和实验代码已经具备正式实验基础，下一阶段最核心的目标就是尽快得到第一版可追溯的 baseline 指标，然后再围绕联合损失和不确定性精修开展消融实验，并把真实模型结果接入 Web 和三维展示。

我的汇报就是这些，谢谢老师和大家。
